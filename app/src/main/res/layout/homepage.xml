<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/bg_gradient3">

    <android.support.design.widget.CoordinatorLayout
        android:id="@+id/codlayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/img1"
            android:layout_width="match_parent"
            android:layout_height="250dp"
            android:src="@drawable/c8"
            android:scaleType="fitXY"
            />

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:layout_alignStart="@+id/codlayout"
            android:background="@drawable/bg_gradient4"
            app:layout_anchor="@id/img1"
            app:layout_anchorGravity="bottom"></RelativeLayout>

        <!--<de.hdodenhof.circleimageview.CircleImageView-->
            <!--android:id="@+id/img2"-->
            <!--android:layout_width="130dp"-->
            <!--android:layout_height="130dp"-->
            <!--android:layout_centerHorizontal="true"-->
            <!--android:layout_marginTop="120dp"-->
            <!--android:src="@drawable/c8"-->
            <!--app:layout_anchor="@id/img1"-->
            <!--app:layout_anchorGravity="bottom|start" />-->
    </android.support.design.widget.CoordinatorLayout>

    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/codlayout"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:background="@drawable/bg_gradient"
        android:text="Emergeny Contact"
        android:padding="10dp"
        android:textColor="@color/aux1"
        android:fontFamily="@font/lato"
        android:textSize="17sp"/>
<TextView
    android:id="@+id/description"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:fontFamily="@font/lato"
    android:textColor="@color/aux6"
    android:layout_centerHorizontal="true"
    android:text="A doctor is always one call or one message away. Save contacts you can contact in times of emergency"
    android:gravity="center"
    android:textSize="20sp"
    android:textStyle="bold"
    android:layout_margin="20dp"
    android:layout_below="@id/title"
    />



</RelativeLayout>